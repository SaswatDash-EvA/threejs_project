import * as THREE from 'three';

const material = new THREE.MeshBasicMaterial({ color: "#45b324", wireframe: false });
const edgeMaterial = new THREE.MeshBasicMaterial({ color: "#ec1313" });

export {material, edgeMaterial};