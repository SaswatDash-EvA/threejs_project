import * as THREE from 'three';

export const edgesMaterial = new THREE.LineBasicMaterial({ color: "red" });
export const handleOuterMaterial = new THREE.MeshStandardMaterial({ color: "cyan", roughness: 0.3, metalness: 0.1 });