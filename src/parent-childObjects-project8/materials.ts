import * as THREE from 'three';

export const partentExtrudeMaterial = new THREE.MeshBasicMaterial({ color: "#f3cfbb", wireframe: false });
export const child1ExtrudeMaterial = new THREE.MeshBasicMaterial({ color: "#6706d6" });
export const child2ExtrudeMaterial = new THREE.MeshBasicMaterial({ color: "gray" });

export const edgeMaterial = new THREE.LineBasicMaterial({ color: "#2438e6" });