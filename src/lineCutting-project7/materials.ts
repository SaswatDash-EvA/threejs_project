import * as THREE from 'three';

export const extrudeMaterial = new THREE.MeshBasicMaterial({ color: "#035be9", wireframe: false });

export const edgeMaterial = new THREE.LineBasicMaterial({ color: "#23c5d1" });