import * as THREE from 'three';

export const handleMaterial = new THREE.MeshBasicMaterial({ color: "#fafddd" });
export const handleHolderMaterial = new THREE.MeshBasicMaterial({ color: "#c26700" });